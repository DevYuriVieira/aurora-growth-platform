import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    iconTabs:{
        alignItems: 'center',
        marginTop: 10,
        width: 80 
    },
    textoTabs:{
        fontSize: 12,
        marginTop: 8,
    }
})